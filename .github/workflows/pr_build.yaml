name: build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  ## Sets environment variable
  PUBLICENV: Available for all jobs in this workflow

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      # - name: Set up JDK 11
      #   uses: actions/setup-java@v2
      #   with:
      #     java-version: 11
      #     distribution: 'adopt'
      # - name: Build with Gradle
      #   run: ./gradlew clean build
      # - name: update ubuntu
      #   run: sudo apt-get update
      # - name: install groovy
      #   run: sudo apt-get install groovy
      # - name: execute groovy sample script
      #   run: groovy .github/workflows/scripts/sample.groovy
      - name: build
        run: echo "Building project"
  unit_test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      # - name: Set up JDK 11
      #   uses: actions/setup-java@v2
      #   with:
      #     java-version: 11
      #     distribution: 'adopt'
      - name: Stating Unit Test
        run: echo "Running unit test"
  db_migration:
    needs: unit_test
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v2
      # - name: Set up JDK 11
      #   uses: actions/setup-java@v2
      #   with:
      #     java-version: 11
      #     distribution: 'adopt'
      - name: DB Migration
        run: echo "Db Migration"
